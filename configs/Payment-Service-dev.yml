spring:
  application:
    name: Payment-Service
  
  datasource:
    url: jdbc:mysql://localhost:3306/orderDb
    username: root
    password: Faisal@1998
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081

  management:
  tracing:
    sampling:
      probability: 1.0

  zipkin:
    tracing:
      endpoint: http://localhost:9411/api/v2/spans
